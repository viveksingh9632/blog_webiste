<%- include('./partials/head') %>

<div class="container">
  <div class="row">
    <div class="col-lg-12 mx-auto mt-4">
      <a href="/add-category" class="btn btn-primary mb-4">Add category</a>

      <% if (message) { %>
      <div
        class="alert alert-dismissible fade show alert-<%= message.type %>"
        role="alert"
      >
        <%= message.text %>
        <button
          class="btn-close"
          type="button"
          data-bs-dismiss="alert"
          aria-label="Close"
        ></button>
        <strong><%= message.message %> </strong>
      </div>
      <% } %>
      <div class="table-responsive">
        <table class="table table-striped text-center">
          <thead>
            <tr class="table-dark">
              <!-- <th>Name</th> -->
              <th>Category</th>
              <th>Title</th>
              <th>Content</th>

              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% categories.forEach((category) => { %>

              <tr>
                <td><%= category.title %></td>
                <td><%= category.content %></td>
                <td><%= category.category %></td>

                <td>
                  <a href="/edit-category/<%= category._id %>" class="text-success"
                    ><i class="fas fa-edit fa-lg mx-1"></i
                  ></a>
                  <a href="/delete-category/<%= category._id %>" class="text-danger"
                    ><i class="fas fa-trash-alt fa-lg mx-1"></i
                  ></a>
                </td>
              </tr>
              <% }); %>

          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<%- include('./partials/footer') %>
